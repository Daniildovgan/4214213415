#include <iostream>
#include <vector>

void addRowToFront(std::vector<std::vector<int>>& arr, std::vector<int>& row) {
    arr.insert(arr.begin(), row);
}

int main() {
    std::vector<std::vector<int>> array2D = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
    std::vector<int> newRow = {10, 11, 12};

    addRowToFront(array2D, newRow);

    for (const auto& row : array2D) {
        for (int num : row) {
            std::cout << num << " ";
        }
        std::cout << std::endl;
    }

    return 0;
}
